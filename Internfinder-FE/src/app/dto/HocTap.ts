export class HocTap {
  school: string;
  dayStart: string;
  dayEnd: string;


  constructor(school: string, dayStart: string, dayEnd: string) {
    this.school = school;
    this.dayStart = dayStart;
    this.dayEnd = dayEnd;
  }
}
